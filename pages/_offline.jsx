// Views import
import OfflineView from '../views/OfflineView/OfflineView';

/**
 * Function represents the page that will show when the user happens to be offline.
 *
 * @returns page's elements
 */
const Offline = () => <OfflineView />;

export default Offline;
